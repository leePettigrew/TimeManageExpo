# deps
node_modules/
.expo/
dist/
build/
web-build/

# env & secrets — never commit real credentials
.env
.env.*
!.env.example
supabase/docker/.env

# test artifacts
supabase/tests/.pgdata/
coverage/

# native build output
app/android/
app/ios/
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Firebase FCM V1 service-account key — SECRET, uploaded to EAS, never commit.
# (google-services.json IS safe to commit — public identifiers only — so it is
#  deliberately NOT ignored; EAS Build needs it in the repo.)
**/google-service-account*.json
**/*serviceaccount*.json
app/fcm-*.json

# OS / editor
.DS_Store
Thumbs.db
